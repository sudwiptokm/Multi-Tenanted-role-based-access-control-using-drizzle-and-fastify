import { z } from 'zod';
export declare type Zennv<S> = {
    dotenv: boolean;
    schema: S;
    data?: Record<string, any>;
};
export declare const main: <S extends z.AnyZodObject>({ dotenv, schema, data, }: Zennv<S>) => z.TypeOf<S>;
export default main;
